language: python
python:
  - 2.7
install:
  - pip install -r requirements.txt
  - pip install pycodestyle
  - pip install pylint

jobs:
  include:
    - stage: PEP8
      script: pycodestyle --ignore=E402 backend

    - stage: Pylint
      script: pylint backend

notifications:
  slack: rv-027py:Xjr17934IAWXxYbexLUuOl89
  email: false